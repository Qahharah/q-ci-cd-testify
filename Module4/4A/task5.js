// 1. Create a variable named day and assign it the current day of the week as a string
const day = "Friday";

// 2. Create another variable named position and assign it the number representing the day of the week
const position = 5;

// 3. Create output string using concatenation
const output = "Today is " + day + ". It is the " + position + "th day of the week.";

// Print the result
console.log(output);
